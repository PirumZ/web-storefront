import ShoppingCart from "../Components/ShoppingCart";

export default function CartPage() {
  return (
    // Probably the most complex page
    // the one where the cart items are displayed and the user can adjust 
    // the amount of the items in their cart
    <div>
      <ShoppingCart />
    </div>
  );
}
